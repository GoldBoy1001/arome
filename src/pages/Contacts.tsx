import Contact from "../components/contacts/contacts";

export default function Contacts() {
  return (
    <section style={{ flex: "1 1 auto" }} className="contact">
      <Contact />
    </section>
  );
}
